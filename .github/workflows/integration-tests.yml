name: Integration Tests
on: [workflow_dispatch, pull_request]
jobs:
  integration:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - run: echo "Wire device/emulator tests after Flutter bootstrap."
