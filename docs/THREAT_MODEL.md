# Threat Model
Cross-user access, API key abuse, malicious share, token theft, supply-chain, log leakage, destructive agent action, Play credential leak. Controls: rules tests, validation, secure storage, CI gates, scoped service account.
