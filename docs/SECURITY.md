# Security
Least privilege, no committed secrets, HTTPS, Rules+emulator tests, AppCheck, Keychain/Keystore, analytics/log allowlist, dependency/secret scanning. Play credential GitHub Secret. Blaze hard deny.
